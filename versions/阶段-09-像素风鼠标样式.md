# 阶段 09 - 像素风鼠标样式

## 目标

将站点常用鼠标样式改造成更符合当前视觉气质的像素风可爱手型光标。

## 已完成

- 为全站默认状态设置像素风手型光标。
- 为 `button`、`a`、`summary` 等常用可点击元素设置手型点击光标。
- 为按下状态补充单独的按压光标变化。
- 保留 `input`、`textarea`、`select` 的文本输入光标，避免影响可用性。

## 相关文件

- `src/styles/_base.scss`

## 说明

这次实现使用内联 SVG data URI，不额外增加静态资源文件，便于后续统一维护和替换。
